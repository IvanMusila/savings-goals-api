{
  "name": "savings-goals-api",
  "version": "1.0.0",
  "description": "A simple JSON server for savings goals",
  "main": "server.js",
  "scripts": {
  "start": "json-server --watch db.json --port $PORT --host 0.0.0.0"
},
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
